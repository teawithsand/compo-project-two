import React from 'react'
import styled from 'styled-components'

const Container = styled.div`
    margin-left: auto;
    margin-right: auto;
`

export const Loading = () => {
    return <Container>Wczytywanie...</Container>
}
